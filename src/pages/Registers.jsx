import React from "react";
import Register from "../components/Register";

const Registers = () => {
	return (
		<>
			<div className="bg-primary flex justify-center items-center w-screen h-screen">
				<Register />
			</div>
		</>
	);
};

export default Registers;
